// Colors
import colors from 'vuetify/lib/util/colors';

const palette = {
    marex: {
        primary: '#87BEC0', // primary main
        primarylight: '#BCDBDC', // primary light
        primarydark: '#549EA0', // primary dark
        secondary: '#383E42', // secondary main
        secondarylight: '#545D63', // secondary light
        secondarydark: '#1C1F21', // secondary dark
        anchor: '#FF4782' // link
    },
};

const theme = {
    ...palette.marex,
};

export default theme;
